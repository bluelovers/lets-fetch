{
  "name": "lets-fetch2",
  "version": "2.1.3",
  "description": "Single and parallel requests with retrying and error handling.",
  "keywords": [],
  "homepage": "https://github.com/queicherius/lets-fetch#readme",
  "bugs": {
    "url": "https://github.com/queicherius/lets-fetch/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/queicherius/lets-fetch.git"
  },
  "license": "MIT",
  "author": "queicherius@gmail.com",
  "main": "./lib/index.js",
  "directories": {
    "lib": "lib",
    "test": "tests"
  },
  "scripts": {
    "build": "tsc -p tsconfig.esm.json && tsc -p tsconfig.json",
    "coverage": "npx nyc npm run test",
    "lint": "npx eslint **/*.ts",
    "ncu": "npx yarn-tool ncu -u",
    "prepublishOnly": "npm run ncu && npm run sort-package-json && npm run test",
    "postpublish": "git commit -m 'publish new version' .",
    "sort-package-json": "npx sort-package-json ./package.json",
    "test": "npm run build && jest --coverage"
  },
  "dependencies": {
    "bluebird": "^3.5.5",
    "cross-fetch": "^3.0.3",
    "fetch-mock": "^7.3.3",
    "lodash-decorators": "^6.0.1",
    "ts-type": "^1.2.2"
  },
  "devDependencies": {
    "@types/fetch-mock": "^7.3.0",
    "@types/jest": "^24.0.13",
    "@types/lodash-decorators": "^4.0.0",
    "fetch-mock": "^7.3.3",
    "jest": "23",
    "ts-jest": "23"
  }
}
